server:
  port: 0
  servlet:
    context-path: '/beerNote/'

spring:
  application:
    name: beerNote
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5433/beerDB?useTimezone=true&serverTimezone=UTC
    username: postgres
    password: admin

eureka:
  instance:
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka